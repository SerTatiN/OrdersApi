package org.top.ordersapi.model.dao.client;

import org.top.ordersapi.model.dao.IDaoBase;
import org.top.ordersapi.model.entity.Client;
import org.top.ordersapi.model.entity.Item;

import java.util.List;
import java.util.Optional;

public interface IDaoClient extends IDaoBase<Client> {
//    List<Client> findAll(); // получить все объекты
//    Optional<Client> findById(Integer id);      // получить объект по id
//    Client save(Client client);  //сохранить
//    Client update(Client client); // обновление
//    Client delete(Integer id); //удалить объект

}
